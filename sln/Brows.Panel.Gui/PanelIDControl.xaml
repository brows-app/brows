<UserControl x:Class="Brows.PanelIDControl"
             x:ClassModifier="internal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:dc="clr-namespace:Brows;assembly=Brows.Panel.Contract"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type={x:Type dc:IPanelID}, IsDesignTimeCreatable=False}">
    <UserControl.Style>
        <Style TargetType="UserControl">
            <Style.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="Background" Value="{DynamicResource PanelID_Default_Background}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="FontFamily" Value="{DynamicResource PanelID_Default_FontFamily}"/>
                    <Setter Property="FontSize" Value="{DynamicResource PanelID_Default_FontSize}"/>
                    <Setter Property="FontWeight" Value="{DynamicResource PanelID_Default_FontWeight}"/>
                    <Setter Property="Foreground" Value="{DynamicResource PanelID_Default_Foreground}"/>
                </Style>
            </Style.Resources>
        </Style>
    </UserControl.Style>
    <TextBox Text="{Binding Value, Mode=OneWay}"/>
</UserControl>
